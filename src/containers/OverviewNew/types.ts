export type TypeStrValNum = { type: string; value: number };
export type IdNumNameStr = { id: number; name: string };
export type DataFilter = {
  population: string;
  promises: string;
  brand: string;
};
